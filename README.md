# README.md

1. `pip install pandas matplotlib`
2. Remove NUL entries from input txt files - see `prompt-log.md`. It is done. (original files with NUL in `18_0904_0908v1`)
3. Run `Meteo.py`
4. Output images in `18_0904_0908out`
